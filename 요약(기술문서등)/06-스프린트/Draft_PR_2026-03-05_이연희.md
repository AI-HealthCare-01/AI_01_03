# Draft PR - 2026-03-05 (이연희)

## 제목
[Sprint01][OCR/DB] 처방전 저장 트랜잭션 강화 및 복약 스케줄 생성 검증

## 목적
- 3/5 목표였던 DB 저장 트랜잭션 구현, 스케줄 생성 로직, 단위 테스트 보강을 마무리한다.

## 변경 사항
- `PrescriptionFlowService` 트랜잭션 강화
  - `in_transaction()` connection을 `using_db`로 명시 전달
  - 처방전/아이템/스케줄 생성이 동일 트랜잭션 경계에서 처리되도록 개선
- 복약 이력/대시보드 API 유지
  - `/api/history`, `/api/dashboard` 계약 테스트 통과 상태 확인
- 단위/통합 테스트 보강
  - 저장 성공 시 생성 건수 검증
  - 스케줄 생성 실패 시 트랜잭션 롤백 검증

## 테스트 결과
- `uv run python -m pytest -q app/tests` 통과
- `uv run python -m pytest -q app/tests/integration_apis/test_integration_contract_apis.py` 통과
- `uv run python -m pytest -q app/tests/integration_apis/test_prescription_flow_service.py` 통과

## 리스크/메모
- 테스트 DB(`test`)가 사전에 생성되지 않은 환경에서는 일시 실패 가능
- 로컬/CI에서 MySQL 준비 후 실행 필요

## 체크리스트
- [x] DB 저장 트랜잭션 구현
- [x] 스케줄 생성 로직 구현
- [x] 단위 테스트 작성
- [x] Draft PR 초안 작성
